# 问题二：中长期封锁下的油价调节与平衡点预测模型

## 1. 建模思路

90 天以后，短期恐慌项逐步减弱，油价主要由四类慢变量决定：

1. 供应侧修复：绕道运输、非海湾国家增产、OPEC 剩余产能释放、阿联酋增产但受运输瓶颈约束。
2. 库存侧缓冲：战略储备释放速度下降，商业库存继续消耗。
3. 需求侧调节：中长期需求价格弹性大于短期弹性，本文取长期弹性 `epsilon=-0.18`。
4. 跳变风险：商业库存接近警戒线后，市场会额外计入断供风险溢价。

因此，本文不用简单线性价格递推，而建立“供需平衡价 + 库存风险溢价 + 价格惯性调整”的动态模型。

## 2. 变量与参数

单位统一为百万桶/日，价格单位为美元/桶。

- `Q0=100`：战前全球供给。
- `D0=100`：战前全球需求。
- `P0=75`：封锁前布伦特基准价格。
- `P90=95.25`：附件数据中 2026 年 6 月 1 日收盘价，作为第 90 天初值。
- `L=16`：基准情景下霍尔木兹封锁造成的中长期有效供应损失。
- `B_t`：绕道运输能力，由第 90 天 2.4 线性增长到第 180 天 3.0。
- `G_t`：其他产油国增产，由第 90 天 1.0 线性增长到第 180 天 4.5。
- `R_t`：战略储备释放，由第 90 天 5.0 线性下降到第 180 天 2.0。
- `C_t`：商业库存释放，释放上限由第 90 天 2.0 下降到第 180 天 0.5。
- `I_t`：商业库存余量，基准情景第 90 天设为 355。
- `lambda=0.16`：价格向均衡价收敛的日调整速度。

## 3. 模型方程

非库存供给为：

```text
S_t^0 = Q0 - L + B_t + G_t + R_t
```

商业库存释放为：

```text
C_t = min(Cap_t, I_t, max(0, D0 - S_t^0))
I_{t+1} = I_t - C_t
```

有效供给：

```text
S_t = S_t^0 + C_t
```

由长期需求弹性得到均衡价格。需求函数为：

```text
D(P) = D0 * (P/P0)^epsilon
```

令 `D(P*)=S_t`，得到：

```text
P_t^* = P0 * (S_t / D0)^(1/epsilon)
```

库存跳变风险溢价为：

```text
Risk_t = 0,                         I_t >= I_min
Risk_t = Rmax * (1 - I_t/I_min)^2,  I_t < I_min
```

其中基准情景 `I_min=90`，`Rmax=38`。最终价格递推：

```text
P_{t+1} = P_t + lambda * (P_t^* + Risk_t - P_t)
```

## 4. 情景设置

| 情景 | 主要假设 |
|---|---|
| 乐观情景 | 有效中断 1400 万桶/日，增产上限 550 万桶/日，战略储备后期仍释放 300 万桶/日，商业库存较充足 |
| 基准情景 | 有效中断 1600 万桶/日，增产上限 450 万桶/日，绕道上限 300 万桶/日 |
| 悲观情景 | 有效中断 1800 万桶/日，增产不足，战略储备降至 100 万桶/日，商业库存接近枯竭并触发风险溢价 |

## 5. 计算结果

由 `code/problem2_oil_model.py` 计算得到：

| 情景 | 第 90 天价格 | 第 180 天预测价格 | 第 180 天供需均衡价 | 第 180 天商业库存 | 第 180 天风险溢价 |
|---|---:|---:|---:|---:|---:|
| 乐观 | 94.72 | 84.36 | 83.91 | 276.25 | 0.00 |
| 基准 | 96.54 | 105.62 | 105.77 | 241.25 | 0.00 |
| 悲观 | 98.63 | 169.09 | 138.90 | 16.25 | 34.45 |

结论：在基准情景下，90-180 天油价不会简单突破 200 美元/桶，而是在供应替代、战略储备、需求弹性共同作用下逐步收敛到约 `105-106` 美元/桶的中长期平衡区间。若封锁强度更高且库存接近耗尽，风险溢价会从平滑调整转为跳变项，价格可能升至 `160-170` 美元/桶区间。

## 6. 代码与输出

- Python 代码：`code/problem2_oil_model.py`
- MATLAB 代码：`code/problem2_oil_model.m`
- Python 测试：`tests/test_problem2_oil_model.py`
- 情景汇总：`results/problem2_summary.csv`
- 价格路径图：`results/problem2_price_paths.svg`
- 三情景日度路径：`results/problem2_baseline_path.csv`、`results/problem2_optimistic_path.csv`、`results/problem2_pessimistic_path.csv`

运行方式：

```bash
python3 code/problem2_oil_model.py
python3 -m unittest tests/test_problem2_oil_model.py
```

MATLAB 中在项目根目录运行：

```matlab
run("code/problem2_oil_model.m")
```
